# 仕様書 - メモ帳（Notes App）

## 1. 概要

React + Vite で作る、ブラウザだけで動くメモ・ノートアプリです。
サーバーは使わず、ブラウザの `localStorage` にメモを保存します。
メモは **Markdown記法**（`# 見出し` や `**太字**` など）で書くことができ、
プレビュー表示に切り替えて仕上がりを確認できます。

難易度: ★★☆（前回作った「タスク帳」より少し発展的な内容です）

## 2. 目的・対象者

- CRUD（作成・読み取り・更新・削除）を、複数の項目を持つデータで練習する
- 検索（フィルタリング）の実装を練習する
- 外部ライブラリ（Markdownパーサー）を導入し、"何でも自作しない" 判断を学ぶ
- 前回の「タスク帳」よりもコンポーネント数を増やし、画面を分割して設計する感覚を養う

## 3. 機能一覧

| # | 機能 | 説明 |
|---|------|------|
| 1 | メモの作成 (Create) | 「新規メモ」ボタンで空のメモを作り、すぐ編集できる状態にする |
| 2 | メモの一覧表示 (Read) | 画面左側に、全メモのタイトルと更新日時の一覧を表示する |
| 3 | メモの選択・閲覧 (Read) | 一覧から選ぶと、右側にMarkdownがプレビュー表示される |
| 4 | メモの編集 (Update) | 「編集」タブでタイトルと本文(Markdown)を書き換えられる。変更は自動保存される |
| 5 | メモの削除 (Delete) | 削除ボタンで、確認ダイアログの後にメモを削除する |
| 6 | 検索 | 検索欄にキーワードを入れると、タイトル・本文に一致するメモだけを一覧に表示する |
| 7 | Markdownプレビュー | 編集中のメモを、Markdown記法を反映したHTMLとして確認できる |
| 8 | データの保存 | メモは `localStorage` に自動保存され、次回アクセス時も残る |

## 4. 画面構成

```
┌───────────────────────┬─────────────────────────────────┐
│  🔍 検索欄               │  タイトル入力欄                     │
│  ＋ 新規メモ              │  [編集] [プレビュー]  最終更新: ...   │
│ ───────────────────    ├─────────────────────────────────┤
│  ● 買い物リスト  8/9      │                                   │
│    今日のTODO   8/8      │   （編集中: Markdownのテキストエリア） │
│    読書メモ     8/7      │   （プレビュー中: HTML表示）          │
│                         │                                   │
│                         │                       [このメモを削除] │
└───────────────────────┴─────────────────────────────────┘
      ← メモ一覧(検索可)              → 選択中メモの編集・プレビュー
```

- 画面幅が狭い（スマートフォンなど）場合は、一覧と編集画面を縦に並べて表示します。

## 5. データの形

1件のメモは、以下の形（JavaScriptのオブジェクト）で表します。

```js
{
  id: "3f2a9c1e-...",        // crypto.randomUUID() で作った一意なID(文字列)
  title: "買い物リスト",        // メモのタイトル
  content: "- 牛乳\n- 卵\n- パン", // 本文(Markdown記法の文字列)
  updatedAt: "2026-08-09T10:00:00.000Z", // 最終更新日時(ISO形式の文字列)
}
```

メモ全体は、この形のオブジェクトを集めた配列として管理します。
配列は「更新日時が新しい順」に並べ替えてから表示します。

## 6. 状態(state)の一覧

| 状態名 | 内容 |
|--------|------|
| `notes` | メモの配列。CRUD操作のたびにここが変化する |
| `selectedId` | 今どのメモを選択中か（未選択なら `null`） |
| `searchQuery` | 検索欄に入力されている文字列 |
| `viewMode` | 選択中メモの表示モード。`"edit"`（編集） / `"preview"`（プレビュー） |

## 7. 使用技術と、その選定理由

| 項目 | 使用するもの | 選定理由 |
|------|--------------|----------|
| フレームワーク | React 18 | 前回同様、UIを部品(コンポーネント)単位で組み立てる |
| ビルドツール | Vite | 高速な開発サーバー |
| データ保存 | localStorage | サーバー不要で、学習用途に手軽 |
| Markdown変換 | `marked`（軽量ライブラリ） | 理由は README.md の「なぜこの実装なのか」に詳しく記載 |
| ID生成 | `crypto.randomUUID()` | 理由は README.md の「なぜこの実装なのか」に詳しく記載 |

## 8. ファイル構成

```
notes-app/
├── README.md              … セットアップ手順 + 設計判断の解説（初心者向け）
├── SPEC.md                … このファイル（仕様書）
├── package.json
├── vite.config.js
├── index.html
└── src/
    ├── main.jsx
    ├── App.jsx                    … 状態管理の中心。CRUD関数をすべてここに集約
    ├── App.css
    ├── index.css
    ├── utils/
    │   └── storage.js              … localStorageの読み書きをまとめた関数群
    └── components/
        ├── Sidebar.jsx              … 左側: 検索欄 + 新規作成ボタン + メモ一覧
        ├── SearchBar.jsx            … 検索入力欄（部品として分離）
        ├── NoteListItem.jsx         … 一覧の1件分の表示
        ├── NoteEditor.jsx           … 右側: 選択中メモの編集/プレビュー
        ├── EmptyState.jsx           … メモが1件も無い/選択されていない時の表示
        └── SpecModal.jsx            … 仕様書をアプリ内に表示するモーダル
```

## 9. 今後拡張するとしたら（対象外・任意）

- タグ付け・カテゴリ分け
- メモの並び替え（作成日順・タイトル順など）
- 画像の貼り付け対応
- 複数端末間の同期（サーバーとの連携が必要）
- Markdownの「分割プレビュー（編集しながら横に同時表示）」
